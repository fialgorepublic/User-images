<%= form_with(model: user_image, local: true) do |form| %>
  <% if user_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_image.errors.count, "error") %> prohibited this user_image from being saved:</h2>

      <ul>
      <% user_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
